# JPA
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/preschool?useSSL=false&allowPublicKeyRetrieval=true
    username: potato
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
